# Scan profile for the vuln-race integration container.
# See vuln-static/hyperz.yaml for the enable-vs-assertion contract
# and the ${ORIGIN_HTTP} placeholder convention.
mode: aggressive

url:
  - "${ORIGIN_HTTP}/spend?amount=60"

checks:
  pollute: true
  enable:
    - race-condition
