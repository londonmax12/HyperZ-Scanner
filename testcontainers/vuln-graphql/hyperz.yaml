# Scan profile for the vuln-graphql integration container.
# See vuln-static/hyperz.yaml for the enable-vs-assertion contract
# and the ${ORIGIN_HTTP} placeholder convention.
mode: aggressive
crawl:
  enabled: true

url:
  - "${ORIGIN_HTTP}/"

checks:
  enable:
    - graphql-audit
